# pyteomics 5.0 uses PEP 604 "X | Y" type-hint syntax, which fails to import
# on Python < 3.10 (this project's target venv runs the macOS system Python
# 3.9 by default). Pin below 5.0 until the venv is moved to Python >= 3.10.
pyteomics<5.0

# reticulate declares its own internal numpy preference on load; installing
# it here just keeps that check quiet, we don't use numpy directly.
numpy

# Exact isotope-pattern calculation (R/isotope_envelope.R), replacing the
# earlier Averagine-based approximation now that every caller has a real
# sequence to compute the exact elemental composition from. Ships prebuilt
# wheels for common platforms (verified: macOS arm64, Python 3.9) -- no
# C++ compiler needed.
IsoSpecPy
